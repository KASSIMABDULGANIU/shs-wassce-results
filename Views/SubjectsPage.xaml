<Page x:Class="SHSWassceResultsMgt.Views.SubjectsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
      Title="Subjects" Background="Transparent">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Subjects" Style="{StaticResource PageTitle}"/>

        <Border Grid.Row="1" Style="{StaticResource FormCard}">
            <WrapPanel>
                <TextBox x:Name="SubjectName"  Width="200" md:HintAssist.Hint="Subject Name *" Margin="0,0,10,8"/>
                <TextBox x:Name="ProgramBox"   Width="160" md:HintAssist.Hint="Program (or Core)" Margin="0,0,10,8"/>
                <CheckBox x:Name="IsCoreBox"   Content="Core Subject" VerticalAlignment="Center" Margin="0,0,16,8"/>
                <TextBox x:Name="SortBox"      Width="70"  md:HintAssist.Hint="Order" Margin="0,0,16,8"/>
                <Button Content="Add"    Click="Add_Click"    Margin="0,0,6,8"/>
                <Button Content="Delete" Click="Delete_Click" Style="{StaticResource MaterialDesignOutlinedButton}" Foreground="Red" Margin="0,0,6,8"/>
                <Button Content="Clear"  Click="Clear_Click"  Style="{StaticResource MaterialDesignFlatButton}"/>
            </WrapPanel>
        </Border>

        <DataGrid Grid.Row="2" x:Name="Grid" AutoGenerateColumns="False"
                  IsReadOnly="True" SelectionChanged="Grid_SelectionChanged"
                  md:DataGridAssist.CellPadding="12,8">
            <DataGrid.Columns>
                <DataGridTextColumn Header="#"        Binding="{Binding Id}"        Width="50"/>
                <DataGridTextColumn Header="Subject"  Binding="{Binding Name}"      Width="220"/>
                <DataGridTextColumn Header="Program"  Binding="{Binding Program}"   Width="160"/>
                <DataGridCheckBoxColumn Header="Core" Binding="{Binding IsCore}"    Width="70"/>
                <DataGridTextColumn Header="Order"    Binding="{Binding SortOrder}" Width="70"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
